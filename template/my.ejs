<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>My Page</title>
  <script src="../../static-js/My.js"></script>
  <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
  <script lang="javascript">
    function showPopup() {
      window.open("findpw.html", "findpassword", "width=800, height=650, left=100, top=50");
    }
  </script>
  <link rel="stylesheet" href="../css/My.css" />
  <link href="https://fonts.googleapis.com/css?family=Noto+Sans+KR:400,500,700&display=swap&subset=korean"
    rel="stylesheet">
</head>

<body>
  <header>
    <h1 class="logo"><a href="/main">Ps timer</a></h1>
    <nav>
      <a href="/findpw.html">비밀번호 재설정</a>
      <a style="color: white;">|</a>
      <a href="/timer">타이머</a>
      <a style="color: white;">|</a>
      <a href="/ranking">랭킹</a>
    </nav>

  </header>
  <main>
    <div class="header">
      <h1>마이페이지</h1>
    </div>
    <div class="content">
      <div class="left">
        <img src="../jpg/my.png" class="my" />
        <span style="margin-top: 25px;"><%= name %></span>
      </div>
      <div class="center">
        <h1>학번 : <small><%= number%></small></h1>
        <h1 style="margin-top: 10px; margin-bottom: 10px;">
          이메일 : <small><%= email%></small>
        </h1>
      </div>
      <div class="right">
        <h1>총 공부한 시간</h1>
        <% var hour = parseInt(time / 3600); var min = parseInt(parseInt(time%3600) / 60); var sec = parseInt(time%3600) % 60; %>
        <h2><%= hour %>시간 <%= min %>분 <%= sec %>초</h2>
      </div>
    </div>
    <div class="study">
      <h1>공부량</h1>
    </div>
    <div class="information">
      <div class="ileft">
        <h1>Today :
          <%console.log(today)%>
          <%var hour1 = parseInt(today / 3600); var min1 = parseInt(parseInt(today%3600) / 60); var sec1 = parseInt(today%3600) % 60;%>
          <small id="dhour"><%= hour1  %> 시간</small>
          <small id="dmin"><%= min1  %> 분</small>
          <small id="dsec"><%= sec1  %> 초</small>
        </h1>
        <br>
        <h1>This Week :
          <%var hour2 = parseInt(week / 3600); var min2 = parseInt(parseInt(week%3600) / 60); var sec2 = parseInt(week%3600) % 60;%>
          <small id="whour"><%= hour2  %> 시간</small>
          <small id="wmin"><%= min2  %> 분</small>
          <small id="wsec"><%= sec2  %> 초</small>

        </h1>
        <br>
        <h1>This Month :
          <%var hour3 = parseInt(month / 3600); var min3 = parseInt(parseInt(month%3600) / 60); var sec3 = parseInt(month%3600) % 60;%>
          <small id="mhour"><%= hour3  %> 시간</small>
          <small id="mmin"><%= min3  %> 분 </small>
          <small id="msec"><%= sec3  %> 초</small>

        </h1>
      </div>
    </div>
  </main>
</body>

</html>